- name: Add MongoDB GPG key
  apt_key:
    url: https://www.mongodb.org/static/pgp/server-6.0.asc
    state: present

- name: Add MongoDB repository
  apt_repository:
    repo: "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/6.0 multiverse"
    state: present

- name: Update apt cache
  apt:
    update_cache: yes

- name: Install MongoDB
  apt:
    name: mongodb-org
    state: present

- name: Enable and start MongoDB service
  systemd:
    name: mongod
    state: started
    enabled: yes

# ✅ Add this new task below
- name: Create MongoDB application user
  command: >
    mongo travelmemory --eval
    'db.createUser({user:"{{ db_user }}", pwd:"{{ db_pass }}", roles:[{role:"readWrite", db:"{{ db_name }}"}]})'
  args:
    creates: /opt/mongo_user_created.flag

- name: Mark MongoDB user created
  file:
    path: /opt/mongo_user_created.flag
    state: touch